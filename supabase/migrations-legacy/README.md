# Legacy Supabase migrations

These files were archived when CodeRocket switched to a single squashed baseline migration for open source distribution.

- Source of truth snapshot: `supabase/baseline.schema.sql`
- Active local baseline: `supabase/migrations/20260501000000_baseline_from_authoritative_schema.sql`

Keep this folder for historical reference only.
New schema work should go through fresh forward migrations in `supabase/migrations/`.
