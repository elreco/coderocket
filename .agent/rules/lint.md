---
trigger: always_on
---

Il faut lancer 'npm run lint' à chaque fin de génération pour checker les erreurs de lint et corriger les erreurs si elles ne sont pas corrigées après que le lint soit terminé. Ne corrige pas les warnings juste les erreurs
